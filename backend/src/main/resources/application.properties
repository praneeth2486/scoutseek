server.port=9191

# Database
spring.datasource.url=jdbc:mysql://localhost:3312/music_player_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Nallolla1!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=ScoutSeekSuperSecretKeyForJWTTokenGeneration2024MustBe256BitsLong
jwt.expiration=86400000

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Upload directory
file.upload-dir=./uploads

# CORS
cors.allowed-origins=http://localhost:5173

# Logging
logging.level.com.scoutseek=DEBUG
logging.level.org.springframework.security=INFO
